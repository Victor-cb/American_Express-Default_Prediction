from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Prediction of default for credit card issuer',
    author='Victor',
    license='MIT',
)
